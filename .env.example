# Public browser configuration only. Never add a service-role key here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-public-publishable-key
VITE_BASE_PATH=/

# Supabase Edge Function secrets (names only; configure values outside Git).
SUPABASE_SERVICE_ROLE_KEY=
ALLOWED_FRONTEND_ORIGINS=http://localhost:5173
JOIN_TOKEN_SIGNING_SECRET=
RECOVERY_TOKEN_SIGNING_SECRET=
